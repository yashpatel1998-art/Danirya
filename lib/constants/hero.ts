/** Shared Hero constants (video delivery architecture). */

export const HERO_TOTAL_FRAMES = 280;

export const HERO_SCROLL_HEIGHT_VH = 500;

export const HERO_SCROLL_HEIGHT_MOBILE_VH = 450;

/** Typography becomes visible after camera settles (architecture only). */
export const HERO_TYPOGRAPHY_SCROLL_THRESHOLD = 0.42;
