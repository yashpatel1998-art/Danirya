/**
 * Separate web GLBs for loader UI.
 * Never wired into the temple pedestal / camera_path hero logo.
 */
export const LOGO_GLB_URL = '/brand/danirya-logo.glb';

/** Cell-fractured duplicate — loading-page explosion only. */
export const LOGO_FRAGMENTS_GLB_URL = '/brand/logo_fragments.glb';

/** Lightweight fallback when WebGL is unavailable. */
export const LOGO_MARK_PNG = '/brand/danirya-mark.png';
