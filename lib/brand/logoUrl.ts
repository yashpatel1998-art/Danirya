/**
 * Separate web GLBs for loader / document UI.
 * Never wired into the temple pedestal / camera_path hero logo.
 */
/** Meshy golden cracked-forge mark — loader + Logo3D (embedded PBR). */
export const LOGO_GLB_URL = '/brand/gf-logo.glb';

/**
 * Cell-fractured duplicate — OpeningLogo explosion only (optional).
 * Still the prior D fracture set until a matching fracture export is provided.
 */
export const LOGO_FRAGMENTS_GLB_URL = '/brand/logo_fragments.glb';

/** Lightweight 2D fallback for tiny marks / route transitions — not the loader. */
export const LOGO_MARK_PNG = '/brand/gf-mark.svg';
