/**
 * Work-hero Jesko: zoom the sanctuary page through the 3D D,
 * then open Case Study from a tiny center iris.
 */
export const GALLERY_JESKO = {
  /** Page still — breathe on the logo shot. */
  holdEnd: 0.12,
  /** Sanctuary page has flown past the camera. */
  zoomEnd: 0.55,
  stageScaleEnd: 14,
  /** Aim through the D counter on the Work hero still. */
  transformOrigin: '50% 46%',
  clipStartPx: 3,
} as const;
