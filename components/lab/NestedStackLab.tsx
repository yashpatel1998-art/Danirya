'use client';

import { NestedStack } from '@/components/nest/NestedStack';

/** /lab scratch — same nest experience as live home. */
export function NestedStackLab() {
  return <NestedStack labChrome />;
}
