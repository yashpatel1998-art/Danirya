'use client';

/**
 * Placeholder entrance brand overlay — restore intended design content.
 * Added to unblock the build; HeroOverlay.tsx imports this as a named export.
 */
export function EntranceBrandOverlay() {
  return null;
}
