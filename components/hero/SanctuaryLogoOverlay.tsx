'use client';

/**
 * Placeholder sanctuary logo overlay — restore intended design content.
 * Added to unblock the build; HeroOverlay.tsx imports this as a named export.
 */
export function SanctuaryLogoOverlay() {
  return null;
}
