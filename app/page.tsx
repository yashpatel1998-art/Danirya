import { Hero } from '@/components/hero/Hero';

/**
 * Phase 2 — temple film is self-contained.
 * Sanctuary CTAs navigate to /work, /studio, /apply (separate pages).
 */
export default function Home() {
  return (
    <main>
      <Hero />
    </main>
  );
}
